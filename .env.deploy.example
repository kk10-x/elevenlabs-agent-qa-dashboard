# Copy to .env on bella:  cp .env.deploy.example .env
# COMPOSE_FILE makes `docker compose` use the standalone deploy file, leaving
# the tracked full-stack docker-compose.yml untouched.
COMPOSE_PROJECT_NAME=elevenlabs-agent-qa-dashboard
COMPOSE_FILE=docker-compose.deploy.yml
